Как работает (в одной ячейке):

Ставит зависимости.

Просит PAT → клонирует mctwork2/tpok003@main.

Открывает диалог загрузки двух файлов (выбери сразу оба).

Просит дату и пароль для ZIP (можно пустой).

Запускает scripts/nbutest.py из репозитория (через app_settings.json).

Скачивает результат напрямую через files.download(...).

Ни ссылок, ни второй ячейки. Если что-то подправить — скажи, поправлю.